format_version: 1.2.0
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

workflows:
  deps-update:
    steps:
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -ex
            rm -rf ./vendor
            GOWS_SYNC_MODE=copy gows glide up
  test:
    steps:
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -ex
            gows go test ./...